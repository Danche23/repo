# 聊天室项目改造总结

> 改造内容：将纯昵称登录升级为「用户名+密码」认证，引入 MySQL 数据持久化 + RBAC 权限表设计

---

## 总览

| 类别 | 文件 | 操作 |
|------|------|:----:|
| 原有文件 | `server/server.go` | 🔧 修改 |
| 原有文件 | `client/client.go` | 🔧 修改 |
| 原有文件 | `go.mod` | 🔧 自动修改（加依赖） |
| 新增文件 | `server/db.go` | ✨ 新增 |
| 新增文件 | `server/auth.go` | ✨ 新增 |
| 新增文件 | `chat_project.sql` | ✨ 新增 |

**完全没有触碰的功能：** 广播消息、私聊 `@用户名`、心跳 PING/PONG、exit 退出

---

## 一、原有文件的改动

### 1. `server/server.go` — 3 处改动

#### 改动①：main 函数加了数据库初始化（第 247-252 行）

```go
// 原来：
fmt.Println("聊天室服务器启动成功...")
go broadcast()
go heartbeat()

// 现在：
fmt.Println("聊天室服务器启动成功...")

err = InitDB()          // ← [新增] 启动时连接 MySQL
if err != nil {
    fmt.Println("数据库初始化失败:", err)
    return
}

go broadcast()
go heartbeat()
```

#### 改动②：process 函数的认证部分整段替换（第 78-135 行）

**删掉的原代码（约 30 行）：**
```go
var name string
for {
    n, err := reader.ReadString('\n')
    name = strings.Trim(n, "\r\n")
    // 验证昵称格式（isValidName）
    // 检查昵称是否已存在（isNameExists）
    // 直到服务器回复 "OK"
}
```

**改成现在的代码：**
```go
// [修改] 读取选择（1=登录 / 2=注册）
choice, _ := reader.ReadString('\n')
// 读取用户名
username, _ := reader.ReadString('\n')
// 读取密码
password, _ := reader.ReadString('\n')

switch choice {
    case "1": // [修改] 登录 — 调用 Login() 查 MySQL 验证 bcrypt 密码
    case "2": // [新增] 注册 — 再读一次确认密码，调用 Register() 写入 MySQL
    default:  // 无效选择
}
```

#### 改动③：删掉了两个废函数

```go
// 原来有：
func isValidName(name string) bool { ... }   // [删除]
func isNameExists(name string) bool { ... }   // [删除]

// 现在：彻底删除
// 按昵称查找用户 getClientByName() 保留（私聊功能还在用）
```

---

### 2. `client/client.go` — 1 处改动

**删掉的原代码（约 40 行）：**
```go
for {
    fmt.Print("请输入你的网名（输入 exit 退出）：")
    n, _ := stdinReader.ReadString('\n')
    // 本地校验昵称格式
    // 发送给服务器
    // 等服务器回复 "OK" 或 "ERR:..."
}
```

**改成现在的代码（第 25-84 行）：**
```go
for {
    fmt.Println("=== 欢迎来到聊天室 ===")
    fmt.Print("1. 登录  2. 注册  请选择：")
    // 发送选择给服务器
    fmt.Print("请输入用户名：")
    conn.Write([]byte(username + "\n"))
    fmt.Print("请输入密码：")
    conn.Write([]byte(password + "\n"))
    // 如果选注册，再多问一次"请再次输入密码"
    // 等服务器回复 "OK" 或 "ERR:..."
}
```

---

### 3. `go.mod` — 自动加依赖

```diff
 module chat
 go 1.25.0
+
+require (
+    github.com/go-sql-driver/mysql v1.10.0   // MySQL 驱动
+    golang.org/x/crypto v0.52.0               // bcrypt 加密
+)
```

---

## 二、新增的文件

### 1. `server/db.go`（全新，36 行）

数据库连接，干三件事：

```
① sql.Open("mysql", "root:123456@tcp(127.0.0.1:3306)/chat_project")
② DB.Ping() 测试能不能连上
③ 设置连接池参数（最多10个连接、5个空闲）
```

### 2. `server/auth.go`（全新，81 行）

两个核心函数：

| 函数 | 做了什么 |
|------|---------|
| `Register(username, password)` | ① `SELECT COUNT(*)` 检查用户名是否存在<br>② `bcrypt.GenerateFromPassword` 加密密码<br>③ `INSERT INTO sys_user` 写入数据库 |
| `Login(username, password)` | ① `SELECT` 按用户名查用户<br>② 检查 `status` 是否启用<br>③ `bcrypt.CompareHashAndPassword` 验证密码 |

### 3. `chat_project.sql`（全新，6 张表）

6 张 RBAC 表：

| 表名 | 用途 |
|------|------|
| `sys_user` | 用户（username, password哈希, nickname, status） |
| `sys_role` | 角色（管理员、普通用户） |
| `sys_permission` | 权限（user:create, role:edit 等） |
| `sys_menu` | 菜单树（首页、系统设置、大屏等） |
| `sys_user_role` | 用户 ↔ 角色（多对多） |
| `sys_role_permission` | 角色 ↔ 权限（多对多） |

同时插入了基础种子数据：2 个角色、7 个菜单、15 个权限。

---

## 三、改动前后流程对比

### 改造前（纯内存）

```
客户端 → 随便输昵称 → 验证空格/重复 → 进聊天室
                                              ↓
                                        所有数据在内存 clients map 里
                                        重启全丢，无安全认证
```

### 改造后（数据库持久化）

```
客户端 → 选 1登录/2注册 → 输用户名 → 输密码
                                              ↓
        服务器查 MySQL 验证（bcrypt 比对密码）
        不通过 → 返回错误，断开连接
        通过 → 进聊天室（同原来的聊天功能）
                                              ↓
                                    用户数据在 MySQL 里
                                    重启不丢，密码加密存储
```
